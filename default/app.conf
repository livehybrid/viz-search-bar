[install]
is_configured = false
state = enabled
build = 1
state_change_requires_restart = false

[ui]
is_visible = false
label = Search Bar Visualization

[launcher]
author = Will Searle (LiveHybrid)
description = A Splunk-styled search bar custom visualization for Dashboard Studio. On submit it fires a drilldown carrying the typed text so a drilldown.setToken handler can capture it into a dashboard token. Built for the Splunk Dashboard Contest 2026.
version = 1.0.0

[package]
id = viz-search-bar
check_for_updates = true

[id]
name = viz-search-bar
version = 1.0.0
