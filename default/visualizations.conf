[search_bar]
label = Search Bar
description = A Splunk-styled search input that sets a dashboard token on submit (via drilldown.setToken). No data source required.
framework_type = studio_visualization
search_fragment = | makeresults | fields - _time
default_height = 56
allow_user_selection = true
