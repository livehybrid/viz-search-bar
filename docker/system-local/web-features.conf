# Studio custom visualizations (framework_type = studio_visualization) are
# behind a feature flag that is OFF by default on Splunk Enterprise. Without
# this the dashboard renders an empty panel and no viz iframe is created.
[feature:dashboard_studio]
activate_studio_extension_framework = true
